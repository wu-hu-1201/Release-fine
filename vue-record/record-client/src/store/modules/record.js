const state = {
    // record: {}
}

const getters = {
    // record: state => state.record
}

const mutations = {
    // SET_RECORD: (state, flag) => {
    //     state.record = flag
    // }
}

const actions = {
    // setRecordInfo ({ commit }, flag) {
    //     commit('SET_RECORD', flag)
    // }
}


export default {
    state,
    getters,
    mutations,
    actions
}